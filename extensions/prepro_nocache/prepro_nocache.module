<?php

/**
 * Implements hook_preprocessor_cache_info().
 */
function prepro_nocache_preprocessor_cache_info() {
	return array(
		'nocache' => array(
			'title' => 'Uncached',
			'description' => 'Recompiled on every pageload',
      'warn' => true
		),
	);
}

/**
 * Implemenets hook_preprocessor_is_cached().
 */
function prepro_nocache_preprocessor_is_cached($file, $type) {
	return true;
}